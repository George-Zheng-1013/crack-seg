# 基础设施外观缺陷智能检测系统 — 项目技术文档

**课题名称**：基础设施外观缺陷智能检测系统

**团队**：第4组

**成员**：郑澈、马俊浩、陈威岐、郑蔡浩、姜星玧

**指导老师**：张恒

**完成时间**：2026年6月

---

## 目录

1. 项目概述
2. 数据工程
3. 算法选型与依据
4. 模型训练过程
5. 超参数调整策略
6. 性能评估结果
7. 系统架构与实现
8. 总结

---

## 1. 项目概述

本项目构建了一套面向基础设施外观缺陷的智能检测系统。系统以计算机视觉为核心，能够对裂缝、破损/剥落、蜂窝麻面、孔洞、钢筋外露、渗水六类常见混凝土表面缺陷进行自动识别、定位、可视化标注和报告导出。

技术路线采用 YOLOv26-Seg 分割模型作为主检测引擎，结合 SigLIP 视觉语言模型进行缺陷成因语义分析，最终通过几何特征与语义匹配的融合决策输出检测结果。系统以 FastAPI 为后端、单页 HTML 为前端，支持图像、批量图像和视频三种输入模式。

---

## 2. 数据工程

### 2.1 数据来源

项目使用的数据集由三个来源合并而成：

| 数据集    | 来源     | 原始类别                                                 | 图片数量（train/val/test） |
| --------- | -------- | -------------------------------------------------------- | -------------------------- |
| crack-seg | 老师提供 | 1类：crack                                               | 3717 / 112 / 200           |
| CUBIT-Det | 老师提供 | 3类：Crack, Spalling, Moisture                           | 304 / 35 / 82              |
| GYU-DET   | 自行搜集 | 6类：Crack, Breakage, Comb, Hole, Reinforcement, Seepage | 7272 / 966 / 963           |

### 2.2 数据合并流程

三个数据集的原始标注格式不同（crack-seg 为分割标注，CUBIT-Det 和 GYU-DET 为检测框标注），需要统一处理后才能用于分割模型训练。合并流程如下：

1. **标注格式统一**：将所有数据集转换为 YOLO 分割格式（class_id + 归一化多边形坐标）。
2. **SAM 模型生成 mask**：对 CUBIT-Det 和 GYU-DET 中仅有检测框标注的图像，使用 Segment Anything Model（SAM）根据边界框提示自动生成像素级分割 mask。
3. **类别映射**：将 CUBIT-Det 的 Spalling 映射为 Breakage，Moisture 映射为 Seepage；crack-seg 的 crack 直接对应 Crack 类别。
4. **数据合并**：三个来源的数据按 train/valid/test 划分合并为统一数据集。

合并工具代码位于 `crack-seg/算法/` 目录。

### 2.3 最终数据集：crack-multi-seg

| 属性           | 值                                                  |
| -------------- | --------------------------------------------------- |
| 路径           | `D:/Develop/dataset/crack-multi-seg/data.yaml`    |
| 类别数         | 6                                                   |
| 类别名称       | Crack, Breakage, Comb, Hole, Reinforcement, Seepage |
| 训练集         | 10,707 张                                           |
| 验证集         | 1,132 张                                            |
| 测试集         | 1,096 张                                            |
| **合计** | **12,935 张**                                 |

数据集相比最初的 crack-seg（约 4,000 张单类别）扩大了约 3 倍，类别覆盖从 1 类扩展到 6 类。

### 2.4 数据增强

训练过程中采用的数据增强策略包括：

- **Mosaic**（概率 1.0）：将 4 张图像拼接为一张，增强小目标和多尺度检测能力。
- **水平翻转**（概率 0.5）：随机水平镜像。
- **缩放**（范围 0.5）：随机缩放图像。
- **随机擦除**（概率 0.4）：随机遮挡部分区域，提升模型对遮挡的鲁棒性。
- **HSV 色彩抖动**：色调偏移 0.015，饱和度变化 0.7，明度变化 0.4。
- **仿射变换**：平移 0.1，无旋转和剪切。

Mosaic 增强在训练最后 10 个 epoch 关闭（`close_mosaic: 10`），让模型在后期专注于精细化学习。

---

## 3. 算法选型与依据

### 3.1 检测框架选型

项目评估了三种主流方案：

| 方案 | 框架        | 优点                                                           | 缺点                                | 结论           |
| ---- | ----------- | -------------------------------------------------------------- | ----------------------------------- | -------------- |
| A    | YOLOv26-Seg | 推理速度快，同时输出 mask、bbox 和置信度，Ultralytics 生态完善 | 细小裂缝边缘精度可能略低            | **选用** |
| B    | U-Net       | 像素级精细分割，医学/工业领域验证充分                          | 需额外后处理生成 bbox，推理速度较慢 | 备选           |
| C    | SegFormer   | Transformer 全局建模能力强                                     | 参数量大，显存占用高，硬件门槛高    | 不采用         |

选择 YOLOv26-Seg 的核心理由：

1. **输出格式兼容**：原生输出 mask 多边形、边界框和置信度，与系统的可视化和报告模块天然匹配，无需额外后处理。
2. **推理速度快**：单张 640×640 图像推理时间约 200-400ms（CPU 环境），满足实时交互需求。
3. **工程部署便捷**：Ultralytics 提供统一的 Python API，训练、验证、导出、推理全流程代码量小。

### 3.2 具体模型版本

训练使用的基础模型为 `yolo26m-seg.pt`（YOLO26-Medium 分割版），这是一个中等规模的分割模型，在精度和速度之间取得较好平衡。模型参数量为 23.6M，FLOPs 为 121.5B，适合在消费级 GPU 上训练。

**YOLO26-Seg 系列模型对比**：

| 模型 | 输入尺寸 | mAPbox 50-95 | mAPmask 50-95 | 参数量 (M) | FLOPs (B) |
|------|----------|--------------|---------------|------------|-----------|
| YOLO26n-seg | 640 | 39.6 | 33.9 | 2.7 | 9.1 |
| YOLO26s-seg | 640 | 47.3 | 40.0 | 10.4 | 34.2 |
| **YOLO26m-seg** | **640** | **52.5** | **44.1** | **23.6** | **121.5** |
| YOLO26l-seg | 640 | 54.4 | 45.5 | 28.0 | 139.8 |
| YOLO26x-seg | 640 | 56.5 | 47.0 | 62.8 | 313.5 |

选择 m 版本在精度和训练成本之间取得平衡。

### 3.3 类别加权损失函数

针对数据集中类别不平衡的问题（Hole、Seepage 类样本数量较少），项目实现了自定义的加权分割损失函数。

**设计思路**：

```python
def make_class_weights(counts, max_weight=3.0):
    """使用 sqrt 逆频率计算类别权重，设置上限避免过度关注少数类"""
    safe_counts = np.maximum(counts, 1)
    weights = np.sqrt(safe_counts.max() / safe_counts)
    weights = np.clip(weights, 1.0, max_weight)
    return torch.tensor(weights, dtype=torch.float32)
```

**关键设计**：

- 使用 `sqrt` 而非直接逆频率，避免少数类权重过大导致模型对多数类精度下降
- 设置权重上限 3.0，平衡各类别的贡献
- 仅对分类 BCE 损失应用权重，边界框和分割损失保持不变

**实现类**：

- `WeightedSegmentationLoss`：继承 `v8SegmentationLoss`，重写分类损失计算
- `WeightedSegmentationModel`：继承 `SegmentationModel`，使用加权损失
- `WeightedSegmentationTrainer`：继承 `SegmentationTrainer`，集成加权模型

### 3.4 缺陷成因分析模型

对于检测到的缺陷区域，系统使用 SigLIP（`google/siglip-so400m-patch14-384`）进行成因语义分析。SigLIP 是 Google 发布的视觉语言模型，通过图文对比学习将图像和文本映射到同一语义空间。

选择 SigLIP 而非 CLIP 的原因：

1. SigLIP 使用 Sigmoid 损失替代 Softmax，在多标签场景下更稳定。
2. 在相同参数量下，SigLIP 的零样本分类精度优于 CLIP。
3. Hugging Face Transformers 库原生支持，集成成本低。

成因分析通过子进程隔离执行，避免 Windows 环境下 OpenCV 与 SigLIP 的线程冲突。主进程将裁剪区域编码为 base64，子进程加载 SigLIP 模型执行图文匹配，结果通过 JSON 返回。

### 3.5 缺陷成因 Prompt 设计

系统为 6 类缺陷设计了共 28 条成因描述（cause_prompts.json），每条包含英文特征描述、中文成因名称和排查建议。以裂缝为例：

| 成因         | 英文特征描述                                                           | 排查建议                                 |
| ------------ | ---------------------------------------------------------------------- | ---------------------------------------- |
| 塑性收缩     | irregular short fine mesh-like cracks on concrete surface              | 结合浇筑后早期养护和失水情况排查         |
| 干燥收缩     | parallel straight cracks or map-like cracking along the long direction | 复核养护条件、环境湿度和构件收缩约束情况 |
| 温度应力     | through crack perpendicular to temperature gradient                    | 结合温差、浇筑厚度和水化热控制记录排查   |
| 不均匀沉降   | diagonal crack with wider upper part and narrower lower part           | 复核基础沉降、支座变形和周边构件位移     |
| 钢筋锈蚀膨胀 | longitudinal crack along reinforcement direction with rust stain       | 检查保护层厚度、钢筋锈蚀和氯离子侵蚀情况 |
| 冻融循环     | map-like cracks with loose edges and surface scaling                   | 结合冻融环境、饱水状态和表面剥落范围排查 |

完整的 6 类 28 条 prompt 覆盖了裂缝、破损/剥落、蜂窝麻面、孔洞、钢筋外露、渗水的典型成因。

---

## 4. 模型训练过程

### 4.1 训练环境

| 项目         | 配置                                              |
| ------------ | ------------------------------------------------- |
| 云平台       | AutoDL                                            |
| GPU          | NVIDIA GeForce RTX 4080 SUPER (32GB)              |
| 训练数据路径 | `/root/autodl-tmp/crack-multi-seg/`             |
| 训练框架     | Ultralytics YOLOv26 (v8.4.51)                     |
| 混合精度     | AMP 开启                                          |

### 4.2 训练超参数

| 参数          | 值     | 说明                     |
| ------------- | ------ | ------------------------ |
| batch_size    | 32     | 每批次样本数             |
| imgsz         | 640    | 输入图像尺寸             |
| lr0           | 0.01   | 初始学习率               |
| lrf           | 0.01   | 最终学习率（lr0 × lrf） |
| momentum      | 0.937  | SGD 动量                 |
| weight_decay  | 0.0005 | 权重衰减                 |
| warmup_epochs | 3.0    | 学习率预热轮数           |
| optimizer     | auto   | 自动选择优化器           |
| patience      | 100    | 早停耐心值（未触发）     |
| close_mosaic  | 10     | 第 10 轮后关闭 Mosaic    |

### 4.3 两组训练实验

为验证类别加权损失的效果，项目进行了两组对比实验：

| 实验组 | 目录                       | 损失函数                         | 训练轮数            |
| ------ | -------------------------- | -------------------------------- | ------------------- |
| 对照组 | `runs/segment/`          | 标准 v8SegmentationLoss          | 16 epochs           |
| 实验组 | `runs/segment_weighted/` | WeightedSegmentationLoss（加权） | 16 + 32 + 32 epochs |

### 4.4 对照组训练过程（标准损失，16 Epochs）

从预训练权重开始，使用标准损失函数训练 16 个 epoch。

**关键 Epoch 指标**：

| Epoch | Box Loss | Seg Loss | Cls Loss | mAP50(B) | mAP50-95(B) | mAP50(M) | mAP50-95(M) |
| ----- | -------- | -------- | -------- | -------- | ----------- | -------- | ----------- |
| 1     | 1.554    | 3.364    | 3.356    | 0.148    | 0.057       | 0.103    | 0.032       |
| 4     | 1.701    | 3.263    | 2.461    | 0.309    | 0.147       | 0.242    | 0.090       |
| 8     | 1.667    | 3.105    | 2.272    | 0.343    | 0.180       | 0.274    | 0.110       |
| 12    | 1.507    | 2.861    | 1.962    | 0.388    | 0.228       | 0.314    | 0.131       |
| 16    | 1.372    | 2.655    | 1.733    | 0.441    | 0.266       | 0.361    | 0.154       |

**损失变化**：

- Box Loss：1.554 → 1.372（下降 11.7%）
- Seg Loss：3.364 → 2.655（下降 21.1%）
- Cls Loss：3.356 → 1.733（下降 48.3%）

### 4.5 实验组训练过程（加权损失）

实验组分三阶段训练，从一开始就使用加权损失函数。

**第一阶段（train，3 Epochs）**：初始测试，验证加权损失训练流程。

**第二阶段（train-2，16 Epochs）**：正式训练。

| Epoch | Box Loss | Seg Loss | Cls Loss | mAP50(B) | mAP50-95(B) | mAP50(M) | mAP50-95(M) |
| ----- | -------- | -------- | -------- | -------- | ----------- | -------- | ----------- |
| 1     | 1.557    | 3.374    | 4.690    | 0.204    | 0.089       | 0.152    | 0.055       |
| 4     | 1.718    | 3.258    | 3.278    | 0.266    | 0.125       | 0.203    | 0.076       |
| 8     | 1.663    | 3.080    | 2.960    | 0.365    | 0.189       | 0.304    | 0.122       |
| 12    | 1.497    | 2.848    | 2.527    | 0.417    | 0.242       | 0.337    | 0.143       |
| 16    | 1.367    | 2.644    | 2.225    | 0.443    | 0.268       | 0.370    | 0.158       |

**第三阶段（train-3，32 Epochs）**：从 train-2 继续训练，达到最佳性能。

| Epoch | Box Loss | Seg Loss | Cls Loss | mAP50(B)        | mAP50-95(B)     | mAP50(M)        | mAP50-95(M)     |
| ----- | -------- | -------- | -------- | --------------- | --------------- | --------------- | --------------- |
| 1     | 1.439    | 2.833    | 2.575    | 0.413           | 0.236           | 0.330           | 0.135           |
| 8     | 1.517    | 2.962    | 2.711    | 0.388           | 0.210           | 0.309           | 0.125           |
| 16    | 1.445    | 2.813    | 2.492    | 0.418           | 0.241           | 0.338           | 0.140           |
| 24    | 1.386    | 2.593    | 2.149    | 0.438           | 0.253           | 0.348           | 0.143           |
| 32    | 1.249    | 2.344    | 1.757    | **0.454** | **0.277** | **0.369** | **0.160** |

**第四阶段（train-4，32 Epochs）**：从 train-3 继续训练，出现过拟合。

| Epoch | Box Loss | Seg Loss | Cls Loss | mAP50(B) | mAP50-95(B) | mAP50(M) | mAP50-95(M) |
| ----- | -------- | -------- | -------- | -------- | ----------- | -------- | ----------- |
| 1     | 1.319    | 2.556    | 2.111    | 0.426    | 0.249       | 0.342    | 0.143       |
| 16    | 1.353    | 2.583    | 2.130    | 0.437    | 0.247       | 0.345    | 0.138       |
| 32    | 1.187    | 2.133    | 1.410    | 0.441    | 0.267       | 0.354    | 0.148       |

**过拟合分析**：

| 阶段           | Train Total Loss | Val Total Loss | 差距            |
| -------------- | ---------------- | -------------- | --------------- |
| train-2 (16ep) | 6.236            | 6.779          | 0.543           |
| train-3 (32ep) | 5.350            | 6.849          | 1.499           |
| train-4 (32ep) | 4.730            | 7.313          | **2.583** |

训练损失持续下降，但验证损失在后期上升，差距从 0.543 扩大到 2.583，表明模型在第四阶段出现明显过拟合。

### 4.6 两组训练对比总结

**相同 Epochs（16）下的性能对比**：

| 实验组                       | Epochs | Box mAP50 | Box mAP50-95 | Mask mAP50 | Mask mAP50-95 | Precision(B) | Recall(B) |
| ---------------------------- | ------ | --------- | ------------ | ---------- | ------------- | ------------ | --------- |
| 对照组（标准损失）           | 16     | 0.441     | 0.266        | 0.361      | 0.154         | 0.517        | 0.439     |
| 实验组（加权损失）           | 16     | 0.443     | 0.268        | 0.370      | 0.158         | 0.497        | 0.454     |
| **差异**                     | -      | +0.002    | +0.002       | +0.009     | +0.004        | -0.020       | +0.015    |

**不同 Epochs 下的性能对比**：

| 实验组                       | Epochs | Box mAP50       | Box mAP50-95    | Mask mAP50      | Mask mAP50-95   | Precision(B) | Recall(B) |
| ---------------------------- | ------ | --------------- | --------------- | --------------- | --------------- | ------------ | --------- |
| 对照组（标准损失）           | 16     | 0.441           | 0.266           | 0.361           | 0.154           | 0.517        | 0.439     |
| **实验组（加权损失）**       | **32** | **0.454**       | **0.277**       | **0.369**       | **0.160**       | 0.503        | 0.467     |
| 实验组（继续训练）           | 64     | 0.441           | 0.267           | 0.354           | 0.148           | 0.509        | 0.462     |

**关键发现**：

1. **相同 Epochs 对比**：在 16 epochs 时，加权损失组的 mAP50(M) 提升 +0.009，Recall 提升 +0.015，但 Precision 下降 -0.020。
2. **最佳模型**：加权损失组在第3阶段（32 epochs）达到最佳性能，mAP50(B)=0.454，mAP50(M)=0.369。
3. **过拟合风险**：继续训练到 64 epochs 后，性能反而下降，出现过拟合。
4. **收敛特性**：加权损失使模型更关注少数类，但也增加了对标注噪声的敏感度。

### 4.7 学习率调度

训练采用线性衰减策略，配合 3 个 epoch 的 warmup：

- **Warmup 阶段**（Epoch 1-3）：学习率从接近 0 线性上升至 0.01。
- **衰减阶段**（Epoch 4-32）：学习率从 0.01 线性衰减至 0.01 × 0.01 = 0.0001。

实际学习率变化（以第3阶段为例）：

| Epoch | 学习率   |
| ----- | -------- |
| 1     | 0.000332 |
| 8     | 0.000783 |
| 16    | 0.000567 |
| 24    | 0.000319 |
| 32    | 0.000041 |

---

## 5. 超参数调整策略

### 5.1 初始配置

训练使用 Ultralytics 的默认超参数作为起点，这些参数在 COCO 等大规模数据集上经过充分验证，具有较好的通用性。

### 5.2 Batch Size 选择

选择 batch_size=32，兼顾训练稳定性和显存占用。较大的 batch size 有助于 Mosaic 增强的效果发挥，同时使梯度估计更稳定。

### 5.3 图像尺寸选择

选择 imgsz=640，这是 YOLO 系列的标准输入尺寸。640×640 在保留足够细节的同时，推理速度可控。对于细小裂缝，更高的分辨率（如 1280）可能带来精度提升，但会显著增加训练和推理成本。

### 5.4 Mosaic 关闭策略

设置 `close_mosaic: 10`，在第 10 个 epoch 后关闭 Mosaic 增强。这样做的原因是：Mosaic 将 4 张图拼接，会改变目标的相对尺度，在训练后期可能干扰模型对真实场景的适应。关闭 Mosaic 后，模型在"干净"的原始图像上进行最后几轮微调。

### 5.5 类别加权策略

针对类别不平衡问题，实验组引入了类别加权损失。权重使用 sqrt 逆频率计算，上限 3.0：

| 类别          | 训练实例数 | 权重  |
| ------------- | ---------- | ----- |
| Crack         | 4,893      | 1.569 |
| Breakage      | 12,038     | 1.000 |
| Comb          | 4,356      | 1.662 |
| Hole          | 818        | 3.000 |
| Reinforcement | 9,504      | 1.125 |
| Seepage       | 3,746      | 1.793 |

可以看到 Breakage 实例数最多（12,038），权重最低（1.000）；Hole 实例数最少（818），权重达到上限（3.000）。

**实验结论**：

- 加权损失在训练前期（16 epochs）对少数类检测有一定帮助
- 但长期训练（32+ epochs）后，模型对少数类的过拟合风险增加
- 最佳策略是使用加权损失训练 32 epochs，然后停止

---

## 6. 性能评估结果

### 6.1 最佳模型性能

最终选用实验组第3阶段（train-3）的 best.pt 作为系统模型，性能指标如下：

| 指标             | 值              | 说明               |
| ---------------- | --------------- | ------------------ |
| Precision (Box)  | 0.503           | 边界框精确率       |
| Recall (Box)     | 0.467           | 边界框召回率       |
| mAP@50 (Box)     | **0.454** | 边界框 mAP@50      |
| mAP@50:95 (Box)  | **0.277** | 边界框 mAP@50:95   |
| Precision (Mask) | 0.469           | 分割掩码精确率     |
| Recall (Mask)    | 0.399           | 分割掩码召回率     |
| mAP@50 (Mask)    | **0.369** | 分割掩码 mAP@50    |
| mAP@50:95 (Mask) | **0.160** | 分割掩码 mAP@50:95 |

### 6.2 与对照组对比

| 指标             | 对照组（16ep） | 实验组（32ep） | 提升幅度 |
| ---------------- | -------------- | -------------- | -------- |
| mAP@50 (Box)     | 0.441          | 0.454          | +2.9%    |
| mAP@50:95 (Box)  | 0.266          | 0.277          | +4.1%    |
| mAP@50 (Mask)    | 0.361          | 0.369          | +2.2%    |
| mAP@50:95 (Mask) | 0.154          | 0.160          | +3.9%    |
| Recall (Box)     | 0.439          | 0.467          | +6.4%    |

加权损失训练在各项指标上均有提升，其中召回率提升最为显著（+6.4%），说明模型对缺陷的检出能力增强。

### 6.3 结果分析

当前最佳模型的 mAP@50 约 45%，处于中等水平。主要原因：

1. **SAM 生成的 mask 质量**：CUBIT-Det 和 GYU-DET 的分割标注由 SAM 自动生成，可能存在边缘不精确、过分割或欠分割的问题，引入了一定的标注噪声。
2. **类别不均衡**：6 类缺陷的样本数量差异较大，小样本类别（如 Hole、Reinforcement）的检测精度可能偏低。
3. **训练已收敛**：模型在第3阶段已基本收敛，继续训练收益有限且存在过拟合风险。

### 6.4 收敛与过拟合判断

| 阶段                            | 收敛状态           | 过拟合风险   | 说明                           |
| ------------------------------- | ------------------ | ------------ | ------------------------------ |
| 对照组 (16ep)                   | 收敛中             | 低           | 训练验证损失同步下降           |
| 实验组 train-2 (16ep)           | 收敛中             | 低           | 与对照组性能接近               |
| **实验组 train-3 (32ep)** | **基本收敛** | **低** | **最佳性能，损失差距小** |
| 实验组 train-4 (64ep)           | 已收敛             | **高** | 验证损失上升，差距扩大         |

**结论**：实验组第3阶段（32 epochs）是最佳平衡点，继续训练未带来性能提升。

---

## 7. 系统架构与实现

### 7.1 整体架构

系统采用前后端一体的轻量架构：

```
┌─────────────────────────────────────────────────┐
│                   前端 (index.html)              │
│  图片上传 / 视频上传 / 实时检测 / 结果展示 / 报告下载  │
└──────────────────────┬──────────────────────────┘
                       │ HTTP / WebSocket
┌──────────────────────▼──────────────────────────┐
│               FastAPI 后端 (main.py)             │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 图像检测  │ │ 批量检测  │ │ 视频检测+追踪    │ │
│  └────┬─────┘ └────┬─────┘ └───────┬──────────┘ │
│       └────────────┼───────────────┘             │
│                    ▼                             │
│  ┌─────────────────────────────────────────────┐ │
│  │         DefectDetector (inference.py)        │ │
│  │  YOLOv26-Seg 推理 → 实例合并 → 几何特征提取  │ │
│  └─────────────────────┬───────────────────────┘ │
│                        ▼                        │
│  ┌─────────────────────────────────────────────┐ │
│  │    CauseAnalyzer (cause_analyzer.py)         │ │
│  │  子进程: SigLIP 图文匹配 → 成因分析          │ │
│  └─────────────────────┬───────────────────────┘ │
│                        ▼                        │
│  ┌──────────────┐ ┌──────────────┐              │
│  │ Visualizer   │ │ Reporter     │              │
│  │ 可视化标注    │ │ Excel/PDF    │              │
│  └──────────────┘ └──────────────┘              │
└─────────────────────────────────────────────────┘
```

### 7.2 核心模块

| 模块         | 文件                       | 职责                                                 |
| ------------ | -------------------------- | ---------------------------------------------------- |
| FastAPI 入口 | `app/main.py`            | 路由定义、会话管理、静态文件服务                     |
| 推理引擎     | `app/inference.py`       | YOLOv26-Seg 加载、推理、实例合并、特征提取、视频追踪 |
| 成因分析     | `app/cause_analyzer.py`  | SigLIP 子进程调度、prompt 管理                       |
| 成因工作器   | `app/cause_worker.py`    | SigLIP 模型加载与图文匹配（子进程）                  |
| 成因 Prompt  | `app/cause_prompts.json` | 6 类 28 条缺陷成因描述                               |
| 可视化       | `app/visualizer.py`      | mask 叠加、边界框绘制、标签渲染                      |
| 报告生成     | `app/reporter.py`        | Excel（openpyxl）和 PDF（reportlab）报告             |

### 7.3 检测流程

1. **图像输入**：用户上传图片或视频帧。
2. **YOLOv26-Seg 推理**：输出每个候选区域的 mask 多边形、边界框、置信度和类别。
3. **实例合并**：使用 Union-Find + IoU 阈值（0.3）将同一裂缝的多个分割结果合并。
4. **几何特征提取**：计算面积占比、细长度、主方向、边界复杂度、骨架长度、分支数量等。
5. **SigLIP 成因分析**：裁剪候选区域，与 28 条 prompt 进行图文匹配，输出各成因的相似度分数。
6. **可视化标注**：在原图上叠加半透明 mask、边界框和类别标签。
7. **报告导出**：生成包含缺陷类型、数量、位置坐标、置信度和成因分析的 Excel/PDF 报告。

### 7.4 视频追踪

视频检测采用逐帧推理 + BoT-SORT 目标追踪的方案：

- 用户设置采样间隔（默认每 5 帧取 1 帧）和最大帧数（默认 200 帧）。
- 每个追踪目标分配唯一 track_id，系统记录其在所有帧中的出现情况。
- 对每个追踪目标，选取置信度最高的帧作为"最佳代表帧"，生成裁剪图用于成因分析。
- 最终输出包含每个缺陷实例的时间线（首次/末次出现时间、最佳帧位置）。

### 7.5 WebSocket 实时检测

系统支持通过 WebSocket 进行实时帧检测，采用两阶段返回策略：

1. **第一阶段**（segmentation_done）：快速返回 YOLO + 实例合并结果，前端立即展示。
2. **第二阶段**（analysis_done）：返回 SigLIP 成因分析补充结果。

这种设计让用户先看到检测框，再看到成因分析，交互体验更流畅。

---

## 8. 总结

本项目完成了一套从数据处理、模型训练到应用系统集成的全流程基础设施缺陷检测系统。主要成果包括：

1. **数据集构建**：整合三个来源的数据集，通过 SAM 模型生成分割标注，构建了包含 12,935 张图像、6 类缺陷的 crack-multi-seg 数据集。
2. **模型训练**：使用 YOLOv26-Seg 在 crack-multi-seg 上进行对比实验：

   - 对照组（标准损失）：16 epochs，mAP50(B)=0.441，mAP50(M)=0.361
   - 实验组（加权损失）：32 epochs，mAP50(B)=0.454，mAP50(M)=0.369
   - 加权损失使召回率提升 6.4%，mAP50 提升 2.9%
3. **类别加权实验**：实现了自定义加权损失函数，实验证明在 32 epochs 时效果最佳，继续训练会导致过拟合。
4. **系统开发**：基于 FastAPI 构建了支持图像/批量/视频三种输入模式的检测系统，集成 SigLIP 成因分析、可视化标注和报告导出功能。
5. **技术创新**：采用分割模型 + 视觉语言模型的融合架构，兼顾缺陷定位精度和成因分析的可解释性。

当前系统的主要局限在于 SAM 生成的 mask 质量不均匀，以及类别不平衡对少数类检测精度的影响。后续工作可集中在数据质量优化、模型规模升级，以及在实际工程场景中的部署测试。

---

## 参考文献

[1] Jocher G, Chaurasia A, Qiu J. Ultralytics YOLO (Version 8.0.0)[EB/OL]. https://github.com/ultralytics/ultralytics, 2023.

[2] Kirillov A, Mintun E, Ravi N, et al. Segment anything[C]//Proceedings of the IEEE/CVF International Conference on Computer Vision. 2023.

[3] Zhai X, Mustafa B, Kolesnikov A, et al. Sigmoid loss for language image pre-training[C]//Proceedings of the IEEE/CVF International Conference on Computer Vision. 2023: 11975-11986.

[4] Radford A, Kim J W, Hallacy C, et al. Learning transferable visual models from natural language supervision[C]//Proceedings of the 38th International Conference on Machine Learning. 2021: 8748-8763.

[5] Liang F, Li Q, Yu H, et al. CrackCLIP: adapting vision-language models for weakly supervised crack segmentation[J]. Entropy, 2025, 27(2): 127.

[6] Zou Q, Zhang Z, Li Q, et al. DeepCrack: learning hierarchical convolutional features for crack detection[J]. IEEE Transactions on Image Processing, 2019, 28(3): 1498-1512.

[7] Aharon S, Orfaig R, Bobrovsky B-Z. BoT-SORT: robust associations multi-pedestrian tracking[J]. arXiv:2206.14651, 2022.
